# M0.5 · the hand in text, and the island — gate receipt · 2026-09-22

*Every number below was printed by a program on this box (RTX 4070 Ti SUPER 16 GB shared with `llama-server`; Windows 11; Python 3.13.2; MSVC 19 / nvcc 13.1 / WSL g++ 13). Reproduce with §7. Nothing here is a claim about a real owner; the gym is the instrument check.*

## 1 · What exists (SPEC r0.2 §5, §7.1, §10.4–10.5, §20.2)

| piece | file | state |
|---|---|---|
| the hand lane's connector | `connectors/direct/hand.py` | `parse_forward` splits the owner's note from the quoted original (Gmail, Outlook, Apple Mail, Thunderbird markers, a bare header block); `HandMailbox` with an mbox/eml backend and a direct IMAP backend (stdlib; read-only; refuses `NETWORK_ABSENT` on the island; **written, not exercised against a live server**); `PasteFolder`; envelope trust operator, payload trust by origin, `declared: true` |
| capture states, the sealed receipt, the reply | `isobard/plane.py` | `SEALED → PARSED → {LINKED, DUPLICATE, UNRESOLVED, REFUSED} → RESOLVED`; the receipt is the sealing row's first eight hex; the typed reply (`sealed · moved · linked · ask? · refused · held`) written to `store/hand/replies/` and to the tape as a `receipt` row; `hand audit` walks every capture to a terminal state (F-SEALED) |
| the three grade cases with strata | `isobard/plane.py` | seen-and-dismissed → a `grade` row on the cell, `stratum: declared`; unseen → a `coverage` row naming the channel; seen-in-state → a `grade` on the row and `DUPLICATE`, no new row |
| the note as testimony | `reflex/questions_note.yaml`, `ReflexClient.compile_note` | five questions; a `correction` row or a candidate `policy` row that is **never applied** |
| UNRESOLVED as the fourth stock | `solver/isobar_field.cu`, `isobard/field.py` | `N_STOCK = 4`; an unresolved row sits in UNRESOLVED or spills to UNPLACED, never in a real cell; O4 over four stocks; **O8c** (priced and holds its rows) with **lie arm 4**; header version 2; carrying weighted by age |
| the unresolved re-join each tick, keyed only | `Plane.tick`, `_rejoin_unresolved`, `_keyed_match` | exact ids (invoice/quote/money ids) first; then the same named actor plus ≥ 2 shared content tokens, or ≥ 3 shared tokens with a rare one; the embedding never resolves |
| dependency-cone invalidation | `isobard/deps.py` | every derived object carries `depends_on`; a resolution invalidates its cone; re-price always; re-foresee recorded as skipped until M1 |
| time as a reducer | `isobard/field.py` | cells before `now` lose their capacity, so a tick alone moves the prices |
| island mode | `Plane`, `isobard/cli.py island --verify` | a `switch` row `island: on`; the IMAP backend refuses `NETWORK_ABSENT`; the frontier seam records `FRONTIER_ABSENT` per tick; every reply that must travel is held as an `effect` with its inverse and `held_reason: NETWORK_ABSENT`; the verify refuses a run labelled island that carries no refusal rows (the lie arm) |
| the gym | `gym/owner_solo.py` | three forwards (one per grade case, with notes), three pastes (links by key · resolves later by exact id · stays), a future mail admitted by a tick, per-item stub truth |
| the instrument, three compilers | `solver/build.cmd`, WSL | 9/9 oracles (O1–O8c) on MSVC, nvcc sm_89 and WSL g++; lie arms 1, 3, 4 fail their oracle correctly |

## 2 · Tests

`python -m pytest tests/` → **40 passed** (M0's 29 + deps 2 + hand 6 + island 3) in 114 s. The purity lint and the sampling lint pass over the new modules.

## 3 · The hand on the gym (stub reflex at competence 1.0)

| plant | rendering | expected | got |
|---|---|---|---|
| Sam's FYI the plane had read as not-commitment, forwarded with "track this…" | forward | seen_dismissed → grade on the cell; UNRESOLVED; note = look_instruction | **as expected** |
| a portal notification the mail lane never carried | forward | unseen → `coverage` row `domain:portal.example`; a new row (LINKED, `creates`) | **as expected** |
| the owner's own Lopez proposal, forwarded with "Lopez is tier 1 — chase this Friday…" | forward | seen_in_state → grade on the row; DUPLICATE, no new row; note = policy_hint recorded as a candidate, never applied | **as expected** |
| "Dan wants the revised loading dock drawings by next Thursday" | paste | LINKED by the entity+lexical key to the Henderson drawings row | **as expected** (method `lexical`) |
| "Q-5521 accepted on their side, start Monday" | paste | UNRESOLVED at scan; RESOLVED by exact id on the tick that admits Priya's "Quote 5521" mail | **as expected** (tick 5, +25 h) |
| "he said maybe Thursday" | paste | UNRESOLVED, stays; re-asked each tick | **as expected** |

Every capture admitted **once** across five ticks (6 captures, 6 replies); `hand audit`: every capture reached a terminal state within budget; replay byte-identical including `folds/captures.json`.

## 4 · The island (F-ISLAND, software cut)

24 synthetic hours in four six-hour ticks with `--island`:

| quantity | value |
|---|---|
| island rows | `on` 1 · `NETWORK_ABSENT` 11 (the IMAP backend once per admission, six held replies) · `FRONTIER_ABSENT` 4 (one per tick) |
| effects | 6, all `held`, each with an inverse, `held_reason: NETWORK_ABSENT` |
| the field on each tick (‖Δv‖) | 17.66 · 21.47 · 31.83 · 14.47 — time alone consumed slack on every tick |
| the X-ray, the captures, the replay | ran; byte-identical over 941 rows |
| `island --verify` | verified; **the lie arm** (a run without the cut, `ISOBAR_NETWORK=present`) is REFUSED |
| end to end | 10.4 s on the CPU reference |

The physical cut (a firewall rule) is the operator's to run; what is verified here is the plane's behaviour under a declared cut.

## 5 · The field with the fourth stock

| quantity | value |
|---|---|
| lattice | N = 19 (15 open rows + 3 unresolved captures + the portal row) · M = 396 (392 + 4 stocks) · 40 iters · 19 ms on the CPU reference |
| stock prices | UNPLACED 0 · UNADJUDICATED 5.26 · WAITING 4.03 · **UNRESOLVED 3.30** — the price of the owner's not-yet-made-sense-of, priced because its budget binds |
| on resolution (tick 5) | ‖Δv‖ 62.3; UNRESOLVED mass falls from three captures to two; the cone of the resolved row invalidated (291 objects, 436 edges in the dependency graph) |
| roofline | working set cache-resident; the CPU reference is the tier (unchanged from M0) |

## 6 · Pending — stated plainly

- **F-HAND's clock starts now** and needs weeks of real forwards: reflex recall on the declared class, measured on passive mail. **F-HABIT** likewise (capture rate up, UNRESOLVED price down).
- **The IMAP and SMTP legs are unexercised live.** The IMAP backend is written on stdlib and refused correctly on the island; the SMTP reply leg is unbuilt — the reply channel at M0.5 is the file and the tape receipt, and the receipt says so.
- **The reply's `moved` line** at scan time carries the stock prices only (no prior field to diff); on a tick it carries the movers. Replies are composed once, at scan; a later resolution is on the tape, not in the reply file (BACKLOG B20).
- **A `tick` verb that rehydrates a plane from the tape** is not built; ticks run in-process after a scan (BACKLOG B17).
- The duplicate floor (0.75) is a placeholder above the gym's noise; the trough is measured on real data (B10).
- Still pending from M0: real pilot owners (F-RECOVERY), 200 owner-labelled emails with the Laya weights (F-JEV).

## 7 · Reproduce

```
cmd /c C:\isobar\solver\build.cmd                                                  (PowerShell; 9 oracles on cl + nvcc)
C:\isobar\solver\build\isobar_field_cpu.exe --selftest --lie 4                     (O8c must FAIL under the lie)
python -m pytest tests/                                                            (40)
python -m isobard.cli gym  --out runs/gym/owner1 --seed 7
python -m isobard.cli scan --owner runs/gym/owner1 --competence 1.0 --island --ticks 4 --tick-hours 6
python -m isobard.cli island --store runs/gym/owner1/store --verify
python -m isobard.cli hand audit --store runs/gym/owner1/store
python -m isobard.cli replay --store runs/gym/owner1/store --verify
python -m isobard.cli scan --owner runs/gym/owner1 --competence 1.0 --store runs/gym/owner1/store_5t --ticks 5 --tick-hours 6 --no-glass
ISOBAR_NETWORK=present python -m isobard.cli scan --owner runs/gym/owner1 --store runs/gym/owner1/store_lie --ticks 1 --no-glass && python -m isobard.cli island --store runs/gym/owner1/store_lie --verify   (must be REFUSED)
```

*Gate verdict: M0.5 PASSES on the instrument (F-ISLAND under a declared cut, F-SEALED, the three grade cases, the keyed re-join, the fourth stock priced with its lie arm); F-HAND and F-HABIT are clocks that start now and need real forwards.*
