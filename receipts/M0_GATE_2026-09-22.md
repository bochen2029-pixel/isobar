# M0 · the X-ray — gate receipt · 2026-09-22

*Every number below was printed by a program on this box (RTX 4070 Ti SUPER 16 GB shared with `llama-server`; Windows 11; Python 3.13.2; MSVC 19 / nvcc 13.1 / WSL g++ 13). Reproduce with the commands in §7. Where a gate step is pending it says so; nothing here is a claim about a real owner.*

## 1 · What exists

| piece | file | state |
|---|---|---|
| the typed objects | `isobard/contracts.py` | round-trip fixtures; an Effect without an inverse is refused; a p outside [0,1] is refused; extra fields refused |
| the tape | `isobard/tape.py` | blake2b-128 chain; one writer per segment (a second process is refused by an OS byte-range lock at offset 2^40); torn tail detected, exposed, truncated before append; a flipped byte is caught at its seq; `replay --verify` rebuilds the folds |
| the field instrument | `solver/isobar_field.cu` | the lift of `ledger_lattice.cu` + the WAITING stock, tier weight, K_PAID, `host_view`, the `--tick` IPC; **8/8 oracles** on cl, nvcc (sm_89) and WSL g++; lie 1 (O4) and lie 3 (O8) fail their oracle correctly this session, lie 2 inherited from the lift |
| the reflex client | `isobard/reflex_client.py` | `SemanticReflex` over `stub` (gym) and `laya` (installed, weights not fetched); 17 questions in one pass; the INERT frame; the deterministic injection pre-filter |
| the join | `isobard/join.py` | exact → lexical → embed (`:8092`, qwen3-embedding-0.6b) → reflex `same_as`; **a discharge or a merge only on a keyed join**; the embedding proposes and never merges |
| promotion | `isobard/promote.py` | deterministic; p becomes the row's distribution; instruction-shaped mail enters only as `contested` |
| the registries | `isobard/registries.py` | people (tiers from `tiers.yaml`, resolution ladder, `mention` on the list lane), money (verdict supersedes mail; the four-figure partition never summed) |
| the bridge | `isobard/field.py` | lattice.bin → `--tick` → Field; the CPU reference under 10⁶ pairs; Δv against the previous tick; the FieldDelta line |
| the scan | `isobard/scan.py` | six finding types, ranked by type · tier · amount · confidence; silence measured against who spoke last on the thread |
| the plane and CLI | `isobard/plane.py`, `isobard/cli.py` | `about · scan · replay · sweep · gym`; every step a tape row; the folds; the receipt |
| the glass | `glass/render.py` | the pressure map, particles with support halos, the three stocks, the money partition, the four bays, the findings with a why-drawer, the arithmetic line |
| connectors | `connectors/direct/` | mbox/eml, ics (recurrence expanded, tentative at p), todo.txt/markdown, vcf, money.csv (read-only verdict) |
| the gym | `gym/owner_solo.py` | 185 messages, 34 calendar instances, 8 list lines, 10 contacts, 2 verdict rows, 10 plants, per-message stub truth |

## 2 · Tests

`python -m pytest tests/` → **29 passed** (contracts 6 · tape 6 · dates 6 · registries 4 · purity 2 · field 2 · gym end-to-end 3) in 67 s.

The purity lint passes: `promote`, `field`, `scan`, `tape`, `contracts` import nothing learned, and no probability is compared to a constant anywhere above the gate.

## 3 · The X-ray on the gym (stub reflex at competence 1.0, verdict connected)

```
ISOBAR X-RAY · as of 2026-09-22 09:00 UTC · capacity coverage WEAK
     $23,400  proposals with no reply (pipeline, not receivable)
      $2,600  confirmed receivable (verdict source)
          $0  invoices that appear overdue* (*payment status not connected)
  APPARENT_OVERDUE_INVOICE   T3 Sam Okafor       2,600 USD  silent 20.4d  p=0.98  → FOLLOW_UP
  SILENT_PROPOSAL            T1 Maria Lopez      23,400 USD  silent 18.0d  p=0.99  → FOLLOW_UP
  PROMISE_PAST_DUE           T2 Ana Ruiz          silent 3.7d  p=0.73  → DO
  CUSTOMER_WAITING           T1 Mom               silent 5.0d  p=0.95  → REPLY
  CUSTOMER_WAITING           T2 Priya Nair        silent 9.0d  p=0.94  → REPLY
  WE_ARE_WAITING             T2 Tom Ferry         silent 11.0d  p=0.97  → FOLLOW_UP
  DUPLICATE                  T1 Dan Henderson     cos 0.83  p=0.83  → MERGE
```

| check | result |
|---|---|
| plants recovered | **7/7**, precision **1.00**, 7 findings, none hidden |
| **F-PAID shape** | INV-1041 (paid by the verdict, no payment mail) never appears; without the verdict it appears as `APPARENT_OVERDUE_INVOICE [not connected]` with recommendation `REVIEW`, never `FOLLOW_UP`; partition without the verdict: apparent-overdue $10,750 labelled, confirmed $0 |
| **F-TIER** (gym) | Mom (tier 1) ranks first among the people waiting; `tier_weight` 2.0 on her row in the lattice; `never_auto_contact` set from policy |
| injection plant | flagged by the pre-filter and the cell (`injection_flagged` 1); the message never becomes an active commitment at any competence (at most `contested`, which never enters the field or the scan); no effect exists at M0 |
| tentative promise | "I should be able to get that to you Thursday" carries `p_hard` < 0.5 |
| the duplicate | the join's candidate at cos 0.83 (full 1024-d) **and** the instrument's K_DUPLICATE stencil (128-d int8, ≥ 0.80) name the same Henderson pair; reported as a candidate, never merged |
| coverage | **WEAK** — 1.2 h per workday booked, 15 % of an 8-hour day; no feasibility claim is made |
| replay | `replay --verify`: chain OK (851 rows), `folds/commitments.json` byte-identical (90 commitments), `folds/findings.json` byte-identical (7) |

## 4 · The field

| quantity | value |
|---|---|
| lattice | N = 15 open rows · M = 395 (14 days × 28 half-hours + 3 stocks) · 40 warm iterations |
| instrument | `isobar_field_cpu.exe` (chosen: 5,925 pairs < 10⁶) · 17 ms per tick; the GPU binary on the same lattice: 44 ms (launch-bound) |
| stock prices | UNPLACED 0 · UNADJUDICATED 5.31 · **WAITING 4.03** (the price of counterparties' silence, priced because the silence budget binds) |
| binding days | Tue 22 · Fri 25 · Wed 23 |
| contradictions | 1 — K_DUPLICATE (the Henderson pair); K_DOUBLEBOOK no longer fires inside stock columns |
| support | es_mean 126: with a mostly free horizon every row can sit almost anywhere; the 3.0 decided/contested split is RAYFORMER's borrowed constant and does not describe a person's week — **re-measure (BACKLOG B10)**; T = 0.25 likely too hot at this radius (M1) |
| roofline | working set **5.75 KB → cache-resident**; the counted per-pair bytes (136 MB) never reach DRAM, so the fraction-of-peak is not a bandwidth claim and is not printed as one. Measured DRAM peak on the card (stream triad, 128 MB per array): 551–612 GB/s across runs. **For one owner the re-price is a CPU job; the card's case is the M1 multiverse** (ROADMAP decision 2026-09-22) |
| joins | 39 exact + 1 lexical of the 40 messages with an antecedent (**100 %**); 90 embed-rung candidate sets on root messages, **0 merges** (by law); 55 none |
| tape | 850 rows, fsync per row, 6.9 s end to end (185 messages) |

## 5 · The competence sweep (three stub seeds; O-MONO)

| competence c | plants recovered (mean of 3) | precision | findings | paid never chased | injection never promoted |
|---|---|---|---|---|---|
| 0.55 | 1.33 / 7 | 0.08 | 15.3 | yes | yes |
| 0.70 | 3.67 / 7 | 0.46 | 8.3 | yes | yes |
| 0.85 | 4.67 / 7 | 0.82 | 6.0 | yes | yes |
| 0.96 | 6.00 / 7 | 0.91 | 6.7 | yes | yes |
| 1.00 | 7.00 / 7 | 1.00 | 7.0 | yes | yes |

Mean recovery is non-decreasing in reflex competence; precision rises with it; the paid invoice is never chased and the instruction-shaped mail never promoted at any competence. **A perfect reflex proves the pipeline; the sweep proves the pipeline degrades with the reflex and not on its own.**

## 6 · Pending — stated plainly

- **F-RECOVERY on real owners** (≥ 60 % of pilot owners find ≥ 3 unknown items at audited precision ≥ 0.80 in ten minutes): the gym is the instrument check, not the claim. Needs pilot owners and their exports.
- **F-JEV** (200 owner-labelled emails; per-question reliability curves): needs the owner's labels and the Laya weights (`convaiinnovations/laya`, ~0.8 GB; installed package, weights not fetched — a download the operator decides). The stub is not a measurement of any reflex.
- **Real mail via Composio**: the API key lives on the bo-brain worker, not on this box; the direct file adapters carry M0.
- **F-INJECT per-lane corpus in CI** is M4; M0's evidence is the plant.
- The support split and the transport temperature at personal radius (B10).

## 7 · Reproduce

```
cmd /c C:\isobar\solver\build.cmd                                  (from PowerShell; builds cl + nvcc, runs both selftests)
python -m pytest tests/                                            (29)
python -m isobard.cli gym   --out runs/gym/owner1 --seed 7
python -m isobard.cli scan  --owner runs/gym/owner1 --competence 1.0
python -m isobard.cli replay --store runs/gym/owner1/store --verify
python -m isobard.cli sweep --owner runs/gym/owner1 --competence 0.55,0.70,0.85,0.96,1.0 --seeds 7,11,13
python -m isobard.cli scan  --owner runs/gym/owner1 --competence 1.0 --no-verdict --store runs/gym/owner1/store_nv
```

*Gate verdict: M0 PASSES on the instrument (the gym); the owner-facing half of the gate (F-RECOVERY, F-JEV) is pending real owners and is recorded as such.*
